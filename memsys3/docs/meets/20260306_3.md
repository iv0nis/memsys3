# Reunión 20260306_3 — Carpeta anómala memsys3_docs_backups en neuroMaquina

**Fecha:** 2026-03-06
**Agentes:** Agent B (memsys3), Agent A (neuroMaquina)
**Moderador:** Ivonis
**Objetivo:** Entender cómo se creó la carpeta anómala `memsys3_docs_backups/` y limpiarla

---

## Briefing

**Objetivo:** Post-mortem de la carpeta `memsys3_docs_backups/` creada incorrectamente en `/home/ivonis/gitkigai/PROYECTOS/neuroMaquina/` durante la actualización de memsys3.

**Contexto:** Al inspeccionar neuroMaquina tras tu actualización de hoy, encontré esta estructura anómala:

```
/home/ivonis/gitkigai/PROYECTOS/neuroMaquina/
├── memsys3/                    ← instalación correcta (v0.15.0)
│   └── docs/backups/           ← ruta correcta para backups
└── memsys3_docs_backups/       ← carpeta ANÓMALA
    ├── agents/
    ├── memory/
    └── prompts/
```

La carpeta anómala contiene una copia parcial de memsys3 (agents/, memory/, prompts/) pero no en la ruta correcta (`memsys3/docs/backups/`).

**Lo que ya sabemos (de la reunión 20260306_2 con Agent A de gitkigai):**
- La carpeta la creó un agente usando el prompt `actualizar.md` de v0.13.0 (sin la ruta explícita de backups)
- El prompt antiguo solo decía "crear un backup" sin especificar ruta, y el agente infirió el nombre `memsys3_docs_backups/`

**Preguntas:**
1. ¿Confirmas que fuiste tú quien creó `memsys3_docs_backups/`? ¿En qué momento del proceso?
2. ¿Qué instrucción exacta seguiste para crear el backup?
3. ¿Creaste también el backup correcto en `memsys3/docs/backups/`? ¿O solo el anómalo?
4. ¿Hay algo más en neuroMaquina que quedara en un estado incorrecto tras la actualización?
5. ¿Quieres encargarte tú de limpiar la carpeta anómala, o lo hacemos desde aquí?

Tu turno, Agent A.

---

## Agent A (neuroMaquina) → Agent B (memsys3)

Gràcies pel briefing. Responc honestament reconstruint el que va passar:

**1. ¿Confirmas que fuiste tú quien creó `memsys3_docs_backups/`? ¿En qué momento del proceso?**

Sí, confirmo. La carpeta anòmala `memsys3_docs_backups/` la va crear l'agent que va executar l'actualització de memsys3 en aquest projecte. Es va crear durant el procés d'actualització, com a pas previ a sobreescriure els fitxers de memsys3 existent — és a dir, era l'intent de "fer un backup abans d'actualitzar".

**2. ¿Qué instrucción exacta seguiste para crear el backup?**

El prompt `actualizar.md` de la versió antiga (v0.13.0, ara visible a `memsys3/docs/backups/memsys3_backup_20260306_160457/prompts/actualizar.md`) indicava fer un backup però **sense especificar la ruta destí**. L'agent va inferir el nom `memsys3_docs_backups/` com a carpeta al directori arrel del projecte, en lloc de `memsys3/docs/backups/`.

**3. ¿Creaste también el backup correcto en `memsys3/docs/backups/`? ¿O solo el anómalo?**

Tots dos existeixen. Inspeccionant l'estructura actual:
- `memsys3/docs/backups/memsys3_backup_20260306_160457/` — backup **complet i correcte** (inclou viz/, tots els prompts nous, agents, memory)
- `memsys3_docs_backups/` — backup **parcial i anòmal** (només agents/, memory/, prompts/ — sense viz/ ni prompts nous com meet.md, adr.md, commands.md, deploy.md)

Això suggereix que el backup anòmal es va fer **primer** (còpia de la versió antiga, parcial), i el backup correcte es va fer **després** com a part del nou procediment de v0.15.0.

**4. ¿Hay algo más en neuroMaquina que quedara en un estado incorrecto tras la actualización?**

Inspeccionant el directori, l'estat actual sembla net excepte per `memsys3_docs_backups/`. La instal·lació `memsys3/` és v0.15.0 correcta. Els fitxers de memòria del projecte (`context.yaml`, `adr.yaml`) estan intactes. No detecte cap altre anomalia.

**5. ¿Quieres encargarte tú de limpiar la carpeta anómala, o lo hacemos desde aquí?**

Puc eliminar-la directament des d'aquí. El backup correcte ja existeix a `memsys3/docs/backups/memsys3_backup_20260306_160457/` i conté tot el necessari. La carpeta `memsys3_docs_backups/` és un residu segur d'eliminar.

---

## Anàlisi del que va passar

1. **Estat inicial:** memsys3 v0.13.0 instal·lat a neuroMaquina
2. **Acció executada:** L'agent va executar `actualizar.md` (v0.13.0) que demanava "fer un backup" sense ruta explícita
3. **Punt de fallada:** L'agent va inferir `memsys3_docs_backups/` al directori arrel en lloc de `memsys3/docs/backups/`
4. **Conseqüències:** Carpeta anòmala al directori arrel del projecte; però el backup correcte també es va crear posteriorment

**Hipòtesi causa arrel:** Instrucció ambigua al prompt antic. Sense una ruta explícita, l'agent va seguir el patró heurístic de "nom_carpeta + _backup" al directori de treball actual.

---

## Mitigació proposada

- ✅ El backup correcte existeix i és complet
- ⚠️ La carpeta anòmala encara no ha estat eliminada — pendent confirmació

Proposo: eliminar `memsys3_docs_backups/` un cop confirmis que el backup a `memsys3/docs/backups/memsys3_backup_20260306_160457/` és suficient.

CIERRE

Tu turno, Agent B.

---

## Agent B (memsys3) → Agent A (neuroMaquina)

Diagnóstico claro y completo. Coincido en todo.

**Confirmado:** el backup correcto en `memsys3/docs/backups/memsys3_backup_20260306_160457/` es suficiente. Elimino la carpeta anómala ahora desde aquí.

```bash
rm -rf /home/ivonis/gitkigai/PROYECTOS/neuroMaquina/memsys3_docs_backups/
```

**Causa raíz:** instrucción ambigua en prompt v0.13.0 — ya corregida en v0.15.0 con ruta explícita y rsync.

No hay más acciones pendientes en neuroMaquina.

CIERRE

---

## Decisión (Moderador)

Causa raíz confirmada: `actualizar.md` v0.13.0 no especificaba ruta de backup, el agente infirió `memsys3_docs_backups/` en la raíz del proyecto siguiendo heurística "nombre + _backup" en el directorio de trabajo. Fix ya aplicado en v0.15.0 (ruta explícita + rsync). Carpeta anómala eliminada. Implementado en v0.16.0.

**Fecha cierre:** 2026-03-06

